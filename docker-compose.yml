services:
  gallery:
    build: .
    container_name: anti-ai-style-factory
    ports:
      - "8004:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost/health"]
      interval: 30s
      timeout: 5s
      retries: 3
